# CLAUDE.md

농산물 가격 변동 요인 분석·예측 시스템. 한국농수산식품유통공사(aT)
AI 프런티어 과정 6조 프로젝트.

이 파일은 저장소를 새로 클론한 세션이 **같은 함정을 다시 밟지 않도록** 하는
용도입니다. 사람이 읽어도 됩니다. 코드에서 바로 확인할 수 있는 내용(구조,
파일 경로, 함수 시그니처)은 적지 않고, **읽어서는 알 수 없는 것**만 적습니다.

## 이 프로젝트의 성격

교육 과정 산출물이고 발표·심사가 있습니다. 그래서 **정확도 수치 자체보다
"왜 이 설계인가"를 방어할 수 있는지**가 중요합니다. 아래 4가지는 의도적으로
코드에 강제해 둔 것이며, 편의를 위해 깨뜨리면 프로젝트의 핵심 논리가 무너집니다.

1. **소비지 가격과 산지 기상을 분리한다.**
   대전 배추값이 오른 원인은 대전 날씨가 아니라 해남 날씨입니다.
   두 지역 개념을 같은 키로 조인하지 마십시오. 화면에도 산지 라벨을 명시합니다.

2. **뉴스와 기상의 역할을 나눈다.**
   뉴스는 가격이 오른 *뒤에* 보도되는 후행 지표입니다. 선행 신호는 기상
   예보가, 사후 설명은 뉴스가 담당합니다. 모든 피처 블록은 `shift(1)` 이후에
   계산합니다 (`features/build.py`). 이걸 빼면 데이터 누수입니다.

3. **나이브 베이스라인을 항상 함께 측정한다.**
   농산물 가격은 임의보행에 가까워 '전일 가격 유지'가 의외로 강합니다.
   `MAPE 12%`는 좋은 수치인지 알 수 없지만 `나이브 대비 오차 18% 감소`는
   명확한 성과입니다. `models/evaluate.py:summarize()` 가 자동 계산합니다.

4. **LLM 이 근거 없는 원인을 말하지 못하게 막는다.**
   근거 ID 부기 강제, 목록 밖 서술 금지, 근거 부족 시 "근거 불충분".
   `explain/llm_summary.py:verify()` 가 사후 검증하고 `cross_check()` 로
   SHAP 상위 요인과 대조합니다.

발표의 핵심 슬라이드는 `models/evaluate.py:ablation()` 이 만드는
**피처 블록별 성능 표**입니다. 실제 데이터로 이 표를 다시 만드는 것이
최대 과제입니다.

## 외부 API — 문서만 보면 반드시 틀리는 것들

4종 모두 실제 호출로 검증했습니다(2026-07-29). 아래는 명세서에 없거나
명세서와 다른 실측 결과입니다.

### 공공데이터포털 (data.go.kr) — ASOS 일자료 + 단기예보 통보문
- **`https` 만 응답합니다.** 명세서 예제는 `http` 인데 `http` 는 **502** 입니다.
- **`Decoding` 키를 `.env` 에 넣으십시오.** `requests` 가 파라미터를 자동
  인코딩하므로 `Encoding` 키를 넣으면 `%2B`→`%252B` 이중 인코딩되어 실패합니다.
- **키가 같아도 서비스별 활용신청이 따로** 필요합니다. 미승인 서비스는
  `resultCode` 오류가 아니라 **HTTP 403** 입니다. 승인 직후엔 전파 지연으로
  잠시 403 이 날 수 있으니 몇 분 뒤 재시도하십시오.
- 오류도 HTTP 200 으로 오고 본문이 XML/HTML 인 경우가 있습니다.
  `weather.py:_check()` 가 잡습니다.
- ASOS 는 전일(D-1)까지만, 그것도 **11시 이후**에 공개됩니다.
- **단기예보는 과거 예보를 재조회할 수 없습니다.** 최근 발표분만 옵니다.
  피처로 쓰려면 **매일 호출해 누적 적재**해야 합니다 (`weather_forecast` 테이블).

### KAMIS
- **`periodRetailProductList` 한 번의 호출이 전 조사지역을 다 돌려줍니다.**
  `p_countycode` 는 **무시**됩니다(응답 echo 가 `null`). 지역별로 나눠
  호출하지 마십시오. 지역 구분은 응답의 `countyname` 으로 합니다.
- `countyname` 실측 24종 = 조사지역 22개 + `평균` + `평년`.
  22개 조사지역이 **17개 시도를 모두 덮습니다** (`kamis.py:COUNTY_TO_SIDO`).
- ★ **`평년`(최근 5년 평균)을 반드시 제외하십시오.** 관측값이 아닙니다.
  섞으면 모델이 과거 평균을 실측처럼 학습합니다. `평균` 은 전국 평균 행이며
  `sgg_code="00"` 으로 들어갑니다 — 지도에서는 빼야 합니다.
- **서버가 구형 TLS** 라 OpenSSL 3.x 기본값이면
  `SSLV3_ALERT_HANDSHAKE_FAILURE` 가 납니다. `kamis.py:_KamisTLSAdapter` 가
  암호 보안수준만 `DEFAULT@SECLEVEL=1` 로 낮춰 해결합니다.
  **`verify=False` 로 끄지 마십시오** — 인증서 검증은 유지해야 합니다.
- 인증키 외에 **요청자 id(`p_cert_id`)** 도 필수입니다. 없으면 빈 응답입니다.
- 잘못된 코드를 주면 `data` 가 dict 가 아니라 **빈 list** 로 옵니다.
  놓치면 '데이터 없음'으로 조용히 묻힙니다.
- **계절 품목 주의**: `dailyPriceByCategoryList` 로 보면 배추 `kind_code=01`(봄)은
  7월에 `-` 이고 `여름(고랭지)` 만 값이 있습니다. 그래서 기간 조회에서는
  품종코드를 지정하지 않습니다.

### 네이버 검색 (뉴스)
- **NCP(네이버 클라우드 플랫폼) 자격증명은 쓸 수 없습니다.**
  `X-NCP-APIGW-*` 헤더용 키로 호출하면 401 입니다. developers.naver.com 에서
  애플리케이션을 등록해 `X-Naver-Client-Id` / `X-Naver-Client-Secret` 을
  받아야 합니다.
- **기간 파라미터가 없고** `start` 최대 1000 입니다. 쿼리 하나로 과거를
  끝없이 파고들 수 없습니다. → **과거 축적은 빅카인즈, 증분만 네이버.**
- **통신사 전재 중복이 실측 35%** 였습니다(5,100 → 3,291건). 제거하지 않으면
  기사량 지표가 사건 크기가 아니라 전재 매체 수를 세게 되어 `art_z30` 이
  무의미해집니다. `news.py:dedupe()` 가 3단계로 처리합니다.
- 현재 규칙 기반 분류는 **"기타"가 65%** 입니다. 검색 쿼리가 넓어서
  무관 기사가 섞입니다. `news.py:QUERIES` 튜닝이 남은 과제입니다.

## 과거에 실제로 있었던 버그 (재발 방지)

- `region_map.STATIONS` 의 진도가 `175` 였습니다. ASOS 일자료 지점 목록에
  **없는 번호**이고 실제로는 `268`(진도군)입니다. 대파는 진도 가중치가 0.4 라
  신호의 40% 가 조용히 사라졌습니다. 나머지 17개는 명세서와 대조 완료.
- `weighted_weather()` / `weighted_forecast()` 가 결측을 0 으로 취급했습니다.
  `groupby.sum()` 이 NaN 을 건너뛰는데 분모의 가중치는 남아서 값이 0 쪽으로
  끌려가, **한여름 최저기온이 0.0℃** 로 나왔습니다. 가중치를 **지표별로 따로**
  합산해야 합니다.
- `features/build._weather_block()` 에만 `shift(1)` 이 없었습니다.
  price/news 블록과 불일치 → 설계 원칙 2 위반.
- `MIN_TRAIN_DAYS` 가 365 여서 `lag_365` 가 첫 폴드에서 전 구간 결측이 되고
  HistGBM 비닝이 죽었습니다. 400 이상이어야 합니다.
- 대시보드에서 지역 선택 시 무한 rerun / 팝업을 닫을 수 없던 문제.
  원인은 팝업이 `region` 을 `None` 으로 비우는데 위젯 선택 상태는 남아서
  다시 열리는 것. `handled_pick` 으로 '직전에 처리한 선택'을 따로 추적합니다.
  **`region` 과 비교하는 방식으로 되돌리지 마십시오.**

## 작업 시 참고

- 지도는 `folium` + OSM 타일이라 **GeoJSON 없이도 동작**합니다
  (`app/map_view.py:SIDO_CENTROIDS`). 경계 파일을 넣으면 자동으로
  Choropleth 가 추가됩니다. 시군구로 내려가려면 좌표표를 확장해야 합니다.
- Supabase 는 선택입니다. `.env` 가 비면 **parquet 폴백**으로 동작합니다.
  로드 시간 단축의 핵심은 DB 도입이 아니라 **읽을 행 수를 줄이는 것**이라,
  사전 집계 테이블(`mart`, `map_layer`)을 만들고 화면은 그것만 읽습니다.
- `data/raw/` 는 원본 응답 캐시입니다. 지워도 재수집되지만 KAMIS 3년치 +
  ASOS 18개 관측소는 시간이 꽤 걸립니다. 캐시가 있으면 재호출하지 않습니다.
- `--mock` 정확도 수치는 **아무 의미 없습니다.** 파이프라인 동작 확인용입니다.
- 새 PC 셋업과 남은 작업 목록은 `README.md` 를 보십시오.
